[tox]
envlist = py{27,34,35,36,37,pypy,pypy3}

[testenv]
passenv = TRAVIS TRAVIS_*
deps =
    pytest
    pytest-cov
    coverage
commands =
    coverage run --branch -m pytest -v
    coverage report -m
